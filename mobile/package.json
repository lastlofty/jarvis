{
  "name": "jarvis-mobile",
  "version": "0.2.0",
  "private": true,
  "description": "Android-обёртка (Capacitor) для PWA Jarvis",
  "scripts": {
    "sync": "node sync-web.js && cap sync android",
    "add:android": "node sync-web.js && cap add android",
    "open:android": "cap open android",
    "build:apk": "node sync-web.js && cap sync android && cd android && gradlew.bat assembleDebug",
    "build:apk:linux": "node sync-web.js && cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
