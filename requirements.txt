# --- LLM (мультипровайдер: GLM основной, Gemini, Ollama) ---
google-generativeai>=0.7.0          # Gemini API (бесплатный тариф)
httpx>=0.27.0                        # HTTP-клиент для GLM и Ollama (runtime)
# mcp>=1.0.0                         # (опц.) MCP-клиент: раскомментируйте для MCP_ENABLED=true

# --- Web-сервер ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9

# --- Системный мониторинг и автоматизация ---
psutil>=5.9.0                        # Запущенные процессы
pygetwindow>=0.0.9                   # Активное окно (Windows)
pywin32>=306; sys_platform == "win32"  # Win API (имя процесса по окну)
pyautogui>=0.9.54                    # Мышь / клавиатура / скриншот
mss>=9.0.1                           # Быстрый захват экрана
Pillow>=10.0.0                       # Работа с изображениями

# --- Хранилище ---
# sqlite3 идёт со стандартной библиотекой Python

# --- Конфигурация и логи ---
python-dotenv>=1.0.0
loguru>=0.7.2
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- Отчёты ---
reportlab>=4.0.0                     # PDF
tabulate>=0.9.0                      # красивые текстовые таблицы

# --- Скиллы, голос, трей (часть опциональна) ---
pyttsx3>=2.90                        # озвучка ответов (TTS), офлайн
ddgs>=6.0                            # веб-поиск (скилл websearch)
keyboard>=0.13.5                     # глобальный хоткей показать/скрыть
qrcode[pil]>=7.4                     # QR-код для подключения телефона
vosk>=0.3.45                         # распознавание речи (STT) + wake-word (модель качается авто)
sounddevice>=0.4.6                   # захват с микрофона для STT

# --- Тестирование и качество ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
black>=24.0.0
ruff>=0.3.0
mypy>=1.9.0
