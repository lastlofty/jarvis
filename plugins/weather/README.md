# Плагин weather — погода

Скилл `get_weather(city)` показывает текущую погоду через бесплатный API
open-meteo (без ключа). Город ищется через геокодинг open-meteo.

## Примеры команд
- «Какая погода в Москве?»
- «Погода в Сочи сейчас»
